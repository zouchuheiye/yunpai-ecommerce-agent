# M6 竞品分析模块开发 — 工作台填写内容

> 供粘贴到团队任务管理网页。本文件只写待办与需求，不写完成状态；
> 真实完成情况见 `docs/tasks/PROGRESS.md`。

---

## 模块信息与确认

**阶段**：`3 业务应用`　**排程方式**：`并行`

### 模块背景

```
开发竞品分析子系统：竞品数据的录入与结构化管理，以及利用模型生成竞品对比
分析报告。这是四个业务模块中相对独立的一个。

本模块的核心约束是"竞品事实必须可追溯、可解释、经人工确认"（项目决策 D-025）：
上游 SKU 对应关系可能把不同容量、套装或型号误判为同款，价格和口碑若直接进入
模型会形成不可追溯的错误建议。因此竞品数据先经过可解释的同款候选评分与人工
裁决，批准后其价格、卖点与脱敏聚合口碑才允许进入分析与 Agent 建议。

本模块建立在项目功能台账既有的以下子功能之上：
· F-301 全域业务模块注册表（模块声明职责、边界、状态与 Agent 工具）
· F-302 统一 Connector SDK 与虚拟接口（能力声明、拉取、动作、读回验证）
· F-304 竞品可信实体、内容证据与监控（可解释同款候选、版本化人工裁决、
  价格与卖点与脱敏聚合口碑、版本化阈值、持久告警、幂等重评、监控 worker）
· F-305 商品与渠道 SKU 事实模块（自有商品作为对比基准的事实来源）
· F-307 指标语义与经营诊断（可量化维度的计算口径）
· F-121 关联虚拟店铺与跨模块场景验收
· F-122 逐场景输入输出证据工作台
· F-310 本地经营与客服管理后台

本模块新增登记：
· F-312 竞品对比分析与报告生成引擎
并扩展 F-304 的数据录入通道与筛选维度。

不保存评论者身份或原始评论内容，不自动改价，不自动调整投放。
```

### 总体需求

```
1. 竞品数据结构化管理（承 F-304 / F-305，扩展 F-304）
   定义竞品数据 Schema，包含标准维度（商品名称、价格、核心卖点、用户评价摘要、
   评分、销量排名）并支持自定义分析维度扩展。提供完整 CRUD、手动表单录入和
   CSV 批量导入（含字段格式校验、数据去重、异常行报错），支持按品类、品牌、
   价格区间、评分范围等多维组合筛选查询，竞品数据可关联 F-305 自有商品作为
   对比基准。去重沿用 F-304 的来源版本与载荷哈希语义（决策 D-014）。

2. 竞品对比分析引擎（承 F-304 / F-307，出 F-312）
   输入自有商品数据与竞品数据列表，执行多维度对比：价格区间对比（纯数据计算，
   按 F-307 口径）、核心卖点差异分析（模型推理）、用户评价情感倾向对比
   （模型推断或规则匹配）、市场定位建议（模型推理）。使用竞品分析 Prompt 模板
   组装上下文，支持用户自定义选择分析维度。
   分析输入只允许使用 F-304 中经人工批准的同款匹配数据（决策 D-025）。

3. 报告呈现与导出（出 F-312）
   报告结构化呈现——先对比表格（价格、评分、卖点一目了然），再文字解读
   （差异分析 + 建议），支持导出为文本或 Markdown 格式。

4. 模块登记与场景验收（承 F-301 / F-121 / F-122 / F-310）
   注册表中 competitive 模块状态与实际能力一致；新增覆盖场景纳入
   simulation-evidence-v1 契约；后台提供可直接操作的分析与报告页面。

约束：可量化维度必须有数值计算支撑，不能是空泛表述；
分析只输出建议，不执行改价、下架或投放动作；
未经人工批准的同款匹配不得进入分析结论；
不保存评论者身份或原始评论内容。
```

### 验收标准

```
【功能验收】
1. 能通过 API 或 CSV 录入竞品商品数据并持久化存储
2. 支持按品类、品牌、价格区间、评分范围筛选查询
3. CSV 批量导入含格式校验和去重，异常行有明确的字段级报错且不导致整批失败
4. 自定义维度可扩展并参与查询
5. 竞品数据可关联 F-305 自有商品，设定对比基准
6. 输入自有商品与至少 3 个竞品数据后，能生成包含价格对比、卖点差异分析、
   评价倾向对比和市场定位建议的结构化分析报告
7. 报告内容有数据支撑且逻辑清晰，价格等可量化维度有数值计算
8. 支持维度勾选（如只看价格和评价，不看卖点）
9. 报告导出为文本或 Markdown 功能可用

【工程验收】
10. 全量测试通过且用例数增长可归因
11. 每项能力具备反证：临时移除后对应测试必须失败
12. 重复导入按来源版本与载荷哈希幂等，旧版本拒绝、同版本冲突可识别
13. 跨租户数据隔离
14. 未批准的同款匹配不进入分析结论，具备可验证的门禁与反证
15. F-301 注册表中 competitive 状态与实际能力一致，且有 F-121 场景覆盖

【边界声明】
不保存评论者身份或原始评论内容，只保存脱敏聚合口碑；
不自动改价、不自动调整投放；
竞品数据来源必须为授权 API、许可供应商、人工录入或显式虚拟样本；
合法竞品与口碑数据源尚未取得，本阶段以虚拟样本完成本机验收，
不构成生产结论。
```

### 交接内容

```
【服务交付】
1. 竞品数据管理服务（F-304 扩展）：数据模型定义 + CRUD API
   + CSV 批量导入接口 + 多维筛选查询接口 + 数据校验逻辑
2. F-312 竞品对比分析与报告生成引擎：分析 API + 多维对比逻辑 + Prompt 组装
   + 模型推理调用 + 情感倾向分析 + 报告格式化 + 导出功能

【配置与结构交付】
3. 竞品数据 Schema 与自定义维度扩展说明
4. F-301 业务模块注册表登记项更新

【文档交付】
5. 模块交付说明与实跑截图（本模块目前尚无 docs/works 交付文档）
6. 项目功能台账登记 F-312 并更新 F-304，进度、版本、验收三份文档同步
```

---

## 任务与进度

各包统一：任务状态 `进行中`。
工作包 1 为独立测试，按模块任务书要求不由开发承接人自行完成，承接人留空。

| # | 任务名称                         | 关联子功能                     |         预计 |         剩余 | 优先级 | 工作类型 | 进度 |
| - | -------------------------------- | ------------------------------ | -----------: | -----------: | -----: | -------- | ---: |
| 1 | 竞品数据准确性与报告质量测试     | F-121 / F-122                  |            8 |            8 |      5 | 测试     |   0% |
| 2 | 竞品数据采集与结构化管理         | F-304 → F-312                 |            6 |            6 |      3 | 开发     |   0% |
| 3 | 竞品数据模型定义与结构化管理服务 | F-304 / F-305 / F-310 扩展     |           16 |            0 |      2 | 开发     | 100% |
| 4 | 竞品对比分析引擎开发             | F-304 / F-307 → F-312         |           20 |           16 |      1 | 开发     |  25% |
| 5 | 竞品多维对比分析与报告生成引擎   | F-121 / F-122 / F-310 → F-312 |           20 |           16 |      4 | 开发     |  25% |
|   | **合计**                   |                                | **70** | **46** |        |          |      |

预计工时含前置基础工作（工作台渠道与灰度可视化），明细见
`docs/tasks/PLATFORM_WORKBENCH.md`。全部工作包已分配：数据层两包由缪海南承接，分析层两包由胡磊承接，
验收测试 8 小时由模块负责人承接。

---

### 工作包 1：竞品数据准确性与报告质量测试

**任务名称**

```
竞品数据准确性与报告质量测试
```

**任务内容**

```
独立验证竞品数据采集、结构化处理、对比分析和报告输出的准确性与可解释性。

关联子功能：
· 承 F-121 关联虚拟店铺与跨模块场景验收——用虚拟店铺竞品候选作为样本来源
· 承 F-122 逐场景输入输出证据工作台——测试结论需能对应到固定输入与预期

覆盖范围：
1. CSV 批量导入的正常与异常路径（列名不匹配、数值格式错误、重复行）
2. 多维筛选查询结果的正确性
3. 同款候选评分的可解释性与误判识别（不同容量、套装、新旧型号）
4. 价格对比数值的人工核对
5. 情感倾向占比与人工判读的一致性
6. 报告结论是否有数据支撑、是否出现未经批准的数据
7. 跨租户数据隔离
8. 拒绝匹配后相关告警资格是否被正确撤销
```

**具体需求**

```
使用代表性竞品样本核对数据、计算和报告结论；
不得由同一开发承接人自行完成测试；
需专门构造易误判的同款样本（同品牌不同容量、单品与套装、新旧型号）；
数值类结论需人工核对；
测试数据需使用显式虚拟标记，不得混入真实竞品数据。
```

**验收标准**

```
提交样本核对记录、测试结果、问题清单、复测结论和验收建议。
```

**交接内容**

```
交付测试报告、样本依据和问题复现材料给模块负责人确认。
```

预计 `8`　剩余 `8`　优先级 `5`　工作类型 `测试`　进度 `0%`　承接人 `模块负责人`

---

### 工作包 2：竞品数据采集与结构化管理

**任务名称**

```
竞品数据采集与结构化管理
```

**任务内容**

```
为分析层准备可直接消费的竞品数据视图。本包**不重复建设数据模型、CSV 导入
通道与通用筛选查询**——那些由工作包 3 交付；本包只做分析侧的取数适配，
确保对比分析引擎拿到的是已批准、已去重、带来源标记的数据集。

关联子功能：
· 承 F-304 竞品可信实体、内容证据与监控——沿用可解释同款候选评分、
  版本化人工裁决与"批准后才进入 Agent"的门禁，本包不新建一套
· 承 F-305 商品与渠道 SKU 事实模块——自有基准商品取自版本化商品事实
· 出 工作包 4 / 5 竞品对比分析与报告生成——本包是它们的取数入口

具体工作：
1. 封装分析侧取数：只返回已批准 match 绑定的竞品数据，未批准数据在入口处就被挡掉
2. 品类 / 品牌维度的分组聚合视图，供对比分析按维度取数（复用工作包 3 的筛选接口，
   不另写查询层）
3. 自定义维度在分析侧的消费：分析请求可指定参与对比的维度子集
4. 与工作包 4 / 5 的取数接口对齐并固化契约

**范围边界（重要）**：CSV 批量导入、列名映射与数值清洗、字段级错误提示、
自定义维度的定义与扩展、按价格区间与评分范围的筛选，**全部属于工作包 3**，
由缪海南实现。本包直接调用，发现问题回报给他，不要自己改。
```

**具体需求**

```
分析侧取数入口必须只返回已批准 match 绑定的数据，未批准数据不得进入下游分析
（决策 D-025）——这条是本包的核心职责；
品类 / 品牌分组聚合复用工作包 3 的筛选接口，不另写查询层；
分析请求可指定参与对比的自定义维度子集；
取数契约需与工作包 4 / 5 对齐并固化，改动要同步；
不保存评论者身份或原始评论内容。
```

**验收标准**

```
1. 分析侧取数只返回已批准 match 绑定的数据，构造一条 rejected 状态的 match，
   确认其价格与口碑不出现在分析结果里
2. 品类 / 品牌分组聚合结果与工作包 3 的筛选接口返回一致（人工核对）
3. 分析请求指定维度子集后，未勾选的维度不参与对比
4. 取数契约与工作包 4 / 5 对齐，分析层按契约调用无需改动
5. 拒绝匹配后相关告警资格被自动撤销
6. 反证：移除批准门禁后"未批准数据不进入分析"的断言必须失败
```

**交接内容**

```
分析侧取数适配层 + 已批准数据门禁校验 + 与工作包 4/5 的取数契约说明
```

预计 `6`　剩余 `6`　优先级 `3`　工作类型 `开发`　进度 `0%`　承接人 `胡磊`

---

### 工作包 3：竞品数据模型定义与结构化管理服务

**任务名称**

```
竞品数据模型定义与结构化管理服务
```

**任务内容**

```
定义竞品数据 Schema，包含标准维度并支持自定义分析维度扩展。开发竞品数据的
完整 CRUD API，支持手动表单录入和 CSV 批量导入（含字段格式校验、数据去重、
异常行报错提示）。实现按品类、品牌、价格区间等多维度筛选和查询能力，
数据持久化到数据库。

关联子功能：
· 承 F-304 竞品可信实体、内容证据与监控——CRUD 与裁决状态机沿用其乐观锁与
  不可变历史语义
· 承 F-305 商品与渠道 SKU 事实模块——对比基准关联使用其 SKU 与版本口径
· 承 F-310 本地经营与客服管理后台——录入、筛选与裁决在后台可直接操作

本包是 M6 数据层的唯一归口：**CSV 批量导入、列名映射与数值清洗、字段级错误
提示、自定义维度的定义与扩展、四种维度的筛选查询，实现全部在本包**。
工作包 2 只做分析侧取数适配，直接调用本包的接口，不重复实现。

具体工作：
1. CSV 批量导入接口：列名映射、数值清洗、字段级错误提示、异常行跳过
2. 多维组合筛选：品类、品牌、价格区间、评分范围（评分筛归一化值，不筛原值）
3. 自定义维度的定义、扩展与参与查询
4. 竞品与自有商品的对比基准关联能力完善
5. 校验规则与错误契约固化
6. 后台页面完成导入、筛选与裁决操作
7. 在 F-301 注册表中核对 competitive 模块状态与实际能力一致
```

**具体需求**

```
1）数据 Schema 需包含标准字段并支持自定义维度扩展，以适应不同品类的分析需求；
2）CSV 导入需处理常见电商平台数据导出格式（列名映射、数值清洗），
   异常行需标记并跳过，不能整批失败；
3）需提供按品类、品牌、价格区间、评分范围等多维组合筛选查询。
   **评分口径：各平台评分制式不同（五分制、十分制、百分制），因此原值与制式
   分开存（`rating_value` + `rating_scale`），派生的归一化评分由代码计算。
   「按评分范围筛选」必须筛归一化值，不得筛原值**——否则五分制的 4.5 分和
   十分制的 4.5 分会被当成同一档，跨平台筛选没有意义。派生值按项目
   "代码算数"的原则由代码现算，不额外落一份可能与原值漂移的冗余列；
4）竞品数据需关联 F-305 自有商品（设定对比基准商品）；
5）导入异常需返回明确的字段级错误提示；
6）跨租户数据不可互读；
7）状态变更使用乐观锁，并发裁决冲突可识别。
```

**验收标准**

```
1. 能通过 API 或 CSV 录入竞品商品数据并持久化存储
2. 支持按品类 / 品牌 / 价格区间 / 评分范围筛选查询；评分筛选走归一化值，
   用五分制与十分制两种来源各造一条样本，确认同一实际水平能被同一区间筛出
3. CSV 批量导入含格式校验和去重，异常行有明确的字段级报错
4. 自定义维度可扩展并参与查询
5. 竞品可关联自有基准商品
6. 跨租户隔离验证通过
7. 并发裁决产生乐观锁冲突而非静默覆盖
```

**交接内容**

```
竞品数据管理服务（数据模型定义 + CRUD API + CSV 批量导入接口
+ 多维筛选查询接口 + 数据校验逻辑）+ 后台操作页面
```

预计 `16`　剩余 `0`　优先级 `2`　工作类型 `开发`　进度 `100%（分支待合并）`　承接人 `缪海南`

---

### 工作包 4：竞品对比分析引擎开发

**任务名称**

```
竞品对比分析引擎开发
```

**任务内容**

```
利用模型对自有商品与竞品进行多维度对比分析。输入：自有商品数据 + 竞品数据
列表。输出分析报告包含：价格区间对比、核心卖点差异分析、用户评价情感倾向对比、
市场定位建议。使用竞品分析 Prompt 模板，支持自定义分析维度选择。

关联子功能：
· 承 F-304 竞品可信实体、内容证据与监控——分析输入只取已批准 match 绑定的
  价格、卖点与脱敏聚合口碑
· 承 F-307 指标语义与经营诊断——价格差异等可量化维度按其口径由代码计算，
  结果带定义版本与证据数量
· 承 F-305 商品与渠道 SKU 事实模块——自有商品侧数据取自版本化商品事实
· 出 F-312 竞品对比分析与报告生成引擎（本包新增登记）

具体工作：
1. 价格区间对比：输出价格差异百分比和区间分布（纯数据计算）
2. 核心卖点差异分析：提取各自卖点并生成差异化解读（模型推理）
3. 用户评价情感倾向对比：输出正面 / 负面 / 中性倾向占比
4. 市场定位建议：基于以上对比生成可执行的定位建议
5. 自定义分析维度选择能力
6. Prompt 组装逻辑，使用竞品分析场景模板
```

**具体需求**

```
分析报告需结构化呈现（表格 + 文字解读）；
评价情感分析可使用模型推断或规则匹配的混合方案，保证本阶段可用；
报告需支持导出；
可量化维度必须有数值计算支撑，不能是空泛表述；
分析输入只允许使用经人工批准的同款匹配数据（决策 D-025）；
不保存评论者身份或原始评论内容；
分析只输出建议，不执行改价、下架或投放动作。
```

**验收标准**

```
1. 输入竞品数据后能生成结构化的对比分析报告，分析内容有数据支撑且逻辑清晰
2. 价格对比有明确的数值计算结果与差异百分比
3. 情感倾向占比输出可用且与人工判读基本一致
4. 市场定位建议引用了具体对比结论而非空泛表述
5. 维度选择生效，未勾选的维度不出现在报告中
6. 反证：移除批准门禁后未批准数据必须能被检出进入了分析
```

**交接内容**

```
F-312 竞品分析引擎服务 + 分析 API + 情感分析逻辑 + 维度选择能力
+ Prompt 组装逻辑
```

预计 `20`　剩余 `16`　优先级 `1`　工作类型 `开发`　进度 `25%`　承接人 `胡磊`

---

### 工作包 5：竞品多维对比分析与报告生成引擎

**任务名称**

```
竞品多维对比分析与报告生成引擎
```

**任务内容**

```
开发竞品对比分析 Pipeline 的报告成型与导出环节：把工作包 4 的多维分析结果
组装为可交付的结构化报告，并完成模块的场景验收与交付文档。

关联子功能：
· 承 F-121 关联虚拟店铺与跨模块场景验收——新增竞品分析报告场景
· 承 F-122 逐场景输入输出证据工作台——场景需符合 simulation-evidence-v1
  契约，运行前展示固定输入与预期，运行后展示逐项断言与真实输出
· 承 F-310 本地经营与客服管理后台——报告在后台可生成、查看与导出
· 出 F-312 竞品对比分析与报告生成引擎

具体工作：
1. 报告结构化呈现：先对比表格（价格 / 评分 / 卖点一目了然），
   再文字解读（差异分析 + 建议）
2. 报告导出：文本 / Markdown 格式
3. 至少 3 个竞品的完整分析报告端到端实跑
4. 新增虚拟店铺场景覆盖竞品分析报告，纳入 F-122 场景证据契约
5. 编写模块交付文档与实跑截图（本模块目前尚无 docs/works 交付文档）
6. 同步项目四份台账：功能清单登记 F-312 并更新 F-304，进度追加历史，
   版本推进与兼容性说明，验收补证据 ID
```

**具体需求**

```
1）分析报告必须基于实际数据，价格对比等可量化维度需有数值计算支撑，
   不能是空泛表述；
2）情感分析可采用模型推断与关键词规则匹配的混合方案，保证本阶段可用；
3）报告需结构化呈现——先对比表格，再文字解读；
4）需支持用户勾选分析维度；
5）需支持报告导出（文本 / Markdown 格式）；
6）分析输入来自结构化竞品数据，Prompt 模板使用竞品分析场景模板；
7）场景断言需可确定性复现，不依赖模型每次输出一致。
```

**验收标准**

```
1. 输入自有商品与至少 3 个竞品数据后，能生成包含价格对比、卖点差异分析、
   评价倾向对比和市场定位建议的结构化分析报告
2. 报告内容有数据支撑且逻辑清晰
3. 支持维度选择和报告导出
4. 端到端实跑截图齐备
5. F-122 场景证据契约扩展后通过
6. 全量测试通过，反证记录完整
7. 四份台账已同步，F-312 已登记并附证据 ID
```

**交接内容**

```
F-312 竞品分析报告服务（报告格式化 + 导出功能）+ 场景验收证据
+ 模块交付文档与实跑截图 + 项目四份台账更新
```

预计 `20`　剩余 `16`　优先级 `4`　工作类型 `开发`　进度 `25%`　承接人 `胡磊`
